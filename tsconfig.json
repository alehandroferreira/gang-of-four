{
  "compilerOptions": {
    "target": "ES2020", // Define a versão do JavaScript que será gerada
    "module": "CommonJS", // Usado pelo Node.js
    "outDir": "./dist", // Diretório de saída para os arquivos compilados
    "rootDir": "./src", // Diretório de origem dos arquivos TypeScript
    "strict": true, // Ativa todas as verificações estritas
    "esModuleInterop": true, // Permite a interoperabilidade com módulos ES
    "skipLibCheck": true, // Ignora checagem de tipos das bibliotecas
    "forceConsistentCasingInFileNames": true // Garante consistência nos nomes de arquivos
  },
  "include": ["src/**/*.ts"], // Inclui todos os arquivos TypeScript dentro da pasta src
  "exclude": ["node_modules"] // Exclui a pasta node_modules
}
